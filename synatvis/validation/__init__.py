"""Three-leg validation harness (CLAUDE.md §7).

The harness is a deliverable, built alongside the modules, not after. The noisy
modules (polya especially) are only shippable once their thresholds are set
against real data — Leg 1 provides that operating point.
"""
